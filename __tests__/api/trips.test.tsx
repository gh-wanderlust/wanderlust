// import React from "react";
// import { shallow } from "enzyme";

// import db from "../../server/db"
// import Trip from "../../server/db/models/tripModel";
// import { request } from "https";

// describe("Trip routes", () => {
//     beforeEach(() => {
//         return db.sync({ force: true })
//     })

//     describe("GET /api/trips", () => {
//         it('displays a list of all trips', async () => {
//             // const userList = await User.

//             // expect(userList[1].id).toEqual(3);
//         });
//     })

//     describe("GET /api/trips/:tripId", () => {
//         it('displays the trip with the specified id', async () => {
            

//             // expect(res.body.email).toEqual(3);
//         });
//     })

//     describe("POST /api/trips", () => {
//         it('responds with an error if trip already exists', async () => {
            

//             // expect().toEqual();
//         });

//         it('saves the new trip to the database', async () => {
//             // const user = await User.
//             // expect(typeof user.listings).toEqual(Array)
//         });
//     })

//     describe("DELETE /api/listings/:listingId", () => {
//         it('deletes the listing by specified id', async () => {
            

//             // expect(res.body.email).toEqual(3);
//         });
//     })

//     describe("UPDATE /api/listings/:listingId", () => {
//         it('updates the listing by specified id', async () => {
            

//             // expect(res.body.email).toEqual(3);
//         });
//     })

// });